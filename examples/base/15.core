class A {
	a:any
	setA(v:any) {
		a = v
	}
}

class B implements A {
	a:int
	setA(v:any) {
	}
}

class C {
	a:A
	b:B
	i:int
	main():int {
		a = new A(1)
		b = <B>a //a.a must be an Int now
		a.setA("foo") // Shouldn't work - type violation in the heap
		return b.a 
	}
}
