class Fruit { say():str { "" } }

class Animal { say():str { "" } }

class Apple implements Fruit { say():str { "apple" } }

class Cat implements Animal { say():str { "cat" } }

class Main {
  f:Fruit
  main():str {
    f = new Apple();
    f = new Cat();
    f.say()
  }
}
