class Fact {
  mul(x:int, y:int):int {
     if0 ( x ) then 
         0 
     else 
      y + mul( x + (-1), y) 
     end 
  }
  fact(x:any):any { 
     if0(x) then 
         1 
      else 
        mul(x, fact(x+(-1))) 
      end
  }
  main():any { fact(5) }
}
