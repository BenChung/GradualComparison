class Fact {
  mul(x:int, y:int):int { 
       if0(x) then 
              y 
       else 
              y+mul(x+(-1), y) 
       end 
   }
  fact(x:int):any { 
      if0(x) then 
              1 
      else 
              mul(x, <int>fact(x+(-1))) 
      end
  }
  main():any { fact(5) }
}
