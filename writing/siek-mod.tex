\documentclass{report}
\usepackage{mathpartir,amsmath,amssymb,xspace,listings,fullpage}


\input{common}

\newcommand{\spec}{\vartriangleright}
\begin{document}

Syntax:

\syntax

Specialization relation:

\begin{mathpar}
\inferrule*[lab=CNSym]{ }{\tau \sqsubseteq \tau}

\inferrule*[lab=CNCons]{ }{\any \sqsubseteq \tau}

\inferrule*[lab=CNClass]{\CLASS\C{\xt{D}}{\ldots}}{D \sqsubseteq C}
\end{mathpar}


Typing rules:

\begin{mathpar}
\inferrule*[lab=TCons]{ \tau \sim \tau' \\ \G \vdash e : \t }{\G \vdash e : \t'}

\TVar 

\TInt

\TUApp

\TApp

\TIf

\TPlus

\TNew

\TCast

\inferrule*[lab=TClass]{ 
  \bar{\G \vdash \f : \t}\\
  \bar{\G \vdash \MTYPE{\m}{\bar{x:\t''}}{\t'} = e} \\ 
  \f:\t \in \xt{D} \implies \f:\t' \in \C \wedge \t' \sqsubseteq \t\\
  \MTYPE{\m}{\bar{x:\t_D''}}{\t_D'}\in \xt{D} \implies \MTYPE{\m}{\bar{x:\t_C''}}{\t_C'} \in \C \wedge \t_C' \sqsubseteq \t_D' \wedge \bar{\t_C'' \sqsubseteq \t_D''}\\
 }{
  \Gdash \CLASS \C{\xt{D}}{\bar{\f:\t},\bar{\MDEF\m{\bar{x:\t''}}{\t'}e}}
  }
\end{mathpar}

\subsection{Class well-formedness}

\begin{mathpar}
\inferrule*[lab=WFField]{\G \vdash \t}{\G\vdash\f:\t}

\inferrule*[lab=WFMeth]{\G \vdash \t \\ \bar{\G \vdash \t'} \\ \G,\this:this(\G),\bar{x:\t'} \vdash e : \t}{\G \vdash \m(\bar{x:\t'}):\t = e}

\inferrule*[lab=WFSelfGet]{f:\t \in fields(\G)}{\G \vdash f():\t = \xt{this}.f}

\inferrule*[lab=WFDelGet]{f:\t \not\in fields(\G) \\ \G \vdash e : \t}{\G \vdash f():\t = e}

\inferrule*[lab=WFSelfSet]{f:\t \in fields(\G)}{\G \vdash f!(\xt{x}:\t):\t = (\xt{this}.f=\xt{x})}

\inferrule*[lab=WFDelSet]{f:\t \not\in fields(\G) \\ \G,\xt{x}:\t \vdash e : \t}{\G \vdash f!(\xt{x}:\t):\t = e}
\end{mathpar}

\subsection{Cast Insertion}

\begin{mathpar}
\inferrule*[lab=CICons]{
  \Gdash e \tlate e' : \t \\
  \t \sim \t'\\
  \t \neq \t'
}{
  \Gdash e \tlate \cast{\t'}e' : \t'
}

\CICast

\CICall

\CICallAny

\CIIf

\CIPlus

\CIVar

\CINew

\end{mathpar}


\section{Dynamic semantics:}



Reduction:

\begin{mathpar}
\EFrame

\ENew

\EInv

\EField

\EAssign

\EIfZ

\EIfNZ

\EPlus

\inferrule*[lab=ECast]{ \s' = \text{cast}(\s, v, \t) }{ \cast{\t}a,\s \ereduce a,\s'}

\ECastInt
\end{mathpar}

\subsection{Casting}

\begin{mathpar}
\inferrule*[lab=CClass]{\s[a \is \{\bar{\f = v} \B \C\}] \\ \C \neq \C \spec \xt{D} \\ \s' = \s[a \is \{\bar{\f = v} \B \C \spec \xt{D}\}]\\
\f:\t \in \C \spec \xt{D} \implies \sigma'' = \text{cast}(\s', v, \t)}{\text{cast}(\s, a, \xt{D}) = \s''}

\inferrule*[lab=CCycle]{\s[a \is \{\bar{\f = v} \B \C\}] \\ \C = \C \spec \xt{D}}{\text{cast}(\s, a, \xt{D}) = \s}

\inferrule*[lab=CInt]{ }{\text{cast}(\s,\xt{int}[n],\xt{\int}) = \s}
\end{mathpar}
\subsection{Meet}
\begin{mathpar}
\inferrule*[]{}{\t \spec \t = \t}

\inferrule*[]{}{\t \spec \any = \t}

\inferrule*[]{}{\any \spec \t = \t}

\inferrule*[]{}{A \spec B = A \Rightarrow B}

\inferrule*[]{}{\bar{B} \Rightarrow A \Rightarrow \bar{C} \spec A = \bar{B} \Rightarrow A \Rightarrow \bar{C}}
\end{mathpar}

\begin{lstlisting}
classMeet(A,B)
  A = class A {(*@\bar{\f_A:\t_A},\bar{m(\bar{x:\t''_A}):\t'_A = e_A}@*)}
  B = class B {(*@\bar{\f_B:\t_B},\bar{m(\bar{x:\t''_B}):\t'_B = e_B}@*)}
  return class A(*@$\Rightarrow$@*)B {
    (*@\bar{\f:\t_A\spec\t_B}@*),
    (*@\bar{m(\bar{x:\t''_A\spec\t''_B}):\t'_A\spec\t'_B = \cast{\t'_A\spec\t'_B}\this.m'(\bar{\cast{\t''_A\spec\t''_B}x})}@*),
    (*@\bar{m'(\bar{x:\t''_A}):\t'_A = e_A}@*)
  }
\end{lstlisting}

classMeet is applied to every string of classes, in left-to-right order
\end{document}
